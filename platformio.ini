; Root PlatformIO wrapper for the current ESPHome-generated build.
; Refresh the generated project with `esphome config esphome/remote_control.yaml`
; or `esphome run esphome/remote_control.yaml` if the device name changes.

[env:esp32dev]
platform = arduino
board = esp32dev
framework = esphome
monitor_speed = 115200